@page "/counter"

<h1>🔢 Contatore</h1>

<p>Valore: @count</p>

<button class="btn btn-primary" @onclick="Incrementa">Incrementa</button>

@code {
    int count = 0;

    void Incrementa()
    {
        count++;
    }
}
